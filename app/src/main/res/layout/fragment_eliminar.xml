<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    android:background="@drawable/fondo_eliminar"
    android:gravity="center">

    <EditText
        android:id="@+id/editTextID"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="ID"
        android:inputType="number"
        android:backgroundTint="#FF4081"
        android:textColor="#FF4081"
        android:textColorHint="#FF4081"/> <!-- Color naranja oscuro -->

    <Button
        android:id="@+id/buttonBorrarRapero"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Borrar Rapero"
        android:layout_gravity="center_horizontal"
        android:background="#FFA726" /> <!-- Añadimos un fondo personalizado al botón -->


</LinearLayout>
